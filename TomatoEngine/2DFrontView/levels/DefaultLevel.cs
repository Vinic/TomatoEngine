using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace TomatoEngine._2DFrontView.levels
{
    public class DefaultLevel : Level
    {
        public override void Build(TomatoMainEngine engine)
        {
            base.Build(engine);
            for ( int x=0; x < 1000; x++)
            {
                for ( int y = 0; y < 1000; y++ )
                {

                }
            }
        }
    }
}
